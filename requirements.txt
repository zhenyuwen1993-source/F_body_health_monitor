# Core runtime + dashboard dependencies.
# Recommended install (also makes the `health_monitor` package importable):
#   pip install -e ".[dev]"
fastapi>=0.110
uvicorn>=0.27
sqlalchemy>=2.0
pandas>=2.1
plotly>=5.20
streamlit>=1.33
python-dateutil>=2.9
python-dotenv>=1.0

# Dev / tests
pytest>=8.0
httpx>=0.27
