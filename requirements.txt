# Core runtime deps (Python 3.14 compatible — use >= so pip picks wheels).
# The web app is pure Streamlit; XML ingest uses lxml.
streamlit>=1.61
pandas>=2.2
plotly>=6.0
lxml>=6.0
altair>=6.0

# Optional: 八字/命理排盘 (lazy-imported in src/insights/advice.py). Safe to omit.
lunar-python>=1.4
