# Normalize line endings: LF in the repo, native on checkout.
* text=auto
# Shell scripts must stay LF so they run on macOS/Linux.
*.sh text eol=lf
# PowerShell scripts as CRLF for Windows.
*.ps1 text eol=crlf
